const app=document.getElementById("app");let page="home";let state=JSON.parse(localStorage.getItem("jkAlgo")||'{"running":false,"symbol":"BTCUSD","strategy":"EMA 9 / EMA 15 Crossover","balance":1000,"orders":[]}');function save(){localStorage.setItem("jkAlgo",JSON.stringify(state))}function money(n){return Number(n||0).toFixed(2)}function render(){document.querySelectorAll("nav button").forEach(b=>b.classList.toggle("active",b.dataset.page===page));if(page==="home")home();if(page==="pnl")pnl();if(page==="ai")ai();if(page==="settings")settings()}function home(){app.innerHTML='<section class="grid"><div class="card"><label>ALGO STATUS</label><div class="value">'+(state.running?"RUNNING":"STOPPED")+'</div></div><div class="card"><label>SYMBOL</label><div class="value">'+state.symbol+'</div></div><div class="card"><label>STRATEGY</label><div class="value" style="font-size:18px">'+state.strategy+'</div></div><div class="card"><label>PAPER BALANCE</label><div class="value">'+money(state.balance)+'</div></div></section><section class="panel section"><h2>Algo Controls</h2><div class="row"><span>Execution</span><span class="status">Paper Only</span></div><div class="row"><span>Signal Logic</span><span>'+state.strategy+'</span></div><div class="row"><span>Position</span><span>FLAT</span></div><div class="actions"><button class="btn" id="toggle">'+(state.running?"Stop Algo":"Start Algo")+'</button></div></section><section class="panel section"><h2>Recent Orders</h2><div class="table-wrap">'+orderTable(5)+'</div></section>';document.getElementById("toggle").onclick=()=>{state.running=!state.running;save();render()}}function orderTable(limit){let a=state.orders.slice(-limit).reverse();if(!a.length)return '<div class="empty">No paper orders yet.</div>';return '<table class="table"><tr><th>Time</th><th>Side</th><th>Price</th><th>P&L</th></tr>'+a.map(o=>'<tr><td>'+o.time+'</td><td>'+o.side+'</td><td>'+money(o.price)+'</td><td>'+money(o.pnl)+'</td></tr>').join("")+'</table>'}function pnl(){let total=state.orders.reduce((s,o)=>s+Number(o.pnl||0),0);app.innerHTML='<section class="grid"><div class="card"><label>TOTAL P&amp;L</label><div class="value '+(total>=0?"positive":"negative")+'">'+money(total)+'</div></div><div class="card"><label>ORDERS</label><div class="value">'+state.orders.length+'</div></div><div class="card"><label>START BALANCE</label><div class="value">1000.00</div></div><div class="card"><label>CURRENT BALANCE</label><div class="value">'+money(state.balance)+'</div></div></section><section class="panel section"><div style="display:flex;justify-content:space-between;align-items:center"><h2>Executed Paper Orders</h2><button class="btn danger" id="clear">Clear Storage</button></div><div class="table-wrap">'+orderTable(100)+'</div></section>';document.getElementById("clear").onclick=()=>{if(confirm("Delete stored paper orders?")){state.orders=[];state.balance=1000;save();render()}}}function ai(){app.innerHTML='<section class="panel"><h2>AI Strategy Builder</h2><p class="muted">Describe your trading logic. The AI layer will convert it into approved strategy rules before paper execution.</p><div class="field"><label>YOUR LOGIC</label><textarea id="logic" placeholder="Example: When EMA 9 crosses above EMA 15, go long. Reverse when it crosses below."></textarea></div><button class="btn" id="build">Build Strategy</button><div class="panel section"><h2>Current Strategy</h2><div id="built" class="muted">'+state.strategy+'</div></div></section>';document.getElementById("build").onclick=()=>{let x=document.getElementById("logic").value.trim();if(!x)return;state.strategy="AI: "+x;save();document.getElementById("built").textContent=state.strategy}}function settings(){app.innerHTML='<section class="panel"><h2>Settings</h2><div class="setting"><div><b>Trading Mode</b><div class="hint">Real orders are disabled in V1.</div></div><span class="status">PAPER</span></div><div class="field"><label>SYMBOL</label><input id="symbol" value="'+state.symbol+'"></div><div class="field"><label>STARTING BALANCE</label><input id="balance" type="number" value="'+state.balance+'"></div><button class="btn" id="saveSettings">Save Settings</button></section>' ;document.getElementById("saveSettings").onclick=()=>{state.symbol=document.getElementById("symbol").value||"BTCUSD";state.balance=Number(document.getElementById("balance").value)||1000;save();render()}}document.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{page=b.dataset.page;render()});render();